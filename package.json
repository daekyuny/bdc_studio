{
  "name": "burndown-studio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --outfile=app.js",
    "dev": "python3 -m http.server 5173",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "jsdom": "^28.1.0"
  }
}
